# Example .env.worker (DO NOT COMMIT real secrets)
# Minimal setup to send real emails with SendGrid (recommended for simplicity)
# Replace the placeholders with your real values.

# SendGrid (preferred - API key and from address)
# SENDGRID_API_KEY="SG.xxxxx..."
# SENDGRID_FROM="Your Name <you@example.com>"

# or, if you prefer SMTP (Gmail), provide these:
# MAIL_USERNAME="your@gmail.com"
# MAIL_PASSWORD="your-app-password"   # App Password if using Gmail
# MAIL_SERVER="smtp.gmail.com"
# MAIL_PORT="587"
# MAIL_USE_TLS="True"

# Redis used by RQ
# REDIS_URL="rediss://default:<token>@<host>:6379"

# Dev convenience (do not enable in production)
# DEV_EMAIL_FALLBACK=1    # write links to tmp/sent_emails.log if SMTP/SendGrid fails
# DEV_RETURN_RESET_LINK=1 # return reset_link in the HTTP /recuperar-senha response (development only)
