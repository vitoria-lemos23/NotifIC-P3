"""
This `wsgi.py` file was a temporary wrapper added to support a Render
deployment experiment. Per the user's request to revert all deployment
changes, the wrapper has been replaced with this harmless placeholder.

If you need the original wrapper back, retrieve it from the commit
history where it was introduced.
"""

raise RuntimeError("wsgi placeholder: deployment WSGI wrapper removed")
